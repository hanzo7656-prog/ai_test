name: 🚀 Keep Render Service Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # هر ۱۰ دقیقه
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  SERVICE_URL: ${{ secrets.SERVICE_URL }}
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: 🏓 Check Basic Health
        id: health_check
        run: |
          echo "🕐 Starting health check at $(date)"
          echo "🔗 Service URL: $SERVICE_URL"
          
          response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" "$SERVICE_URL/api/health/ping")
          http_status=$(echo "$response" | grep "HTTP_STATUS" | cut -d':' -f2)
          
          if [ "$http_status" -eq 200 ]; then
            echo "✅ Basic health check passed"
          else
            echo "❌ Basic health check failed: HTTP $http_status"
            exit 1
          fi

      - name: 📊 Check Full Health Status
        run: |
          echo "🔍 Checking full health status..."
          
          response=$(curl -s "$SERVICE_URL/api/health/status")
          health_score=$(echo "$response" | grep -o '"health_score":[0-9]*' | cut -d':' -f2)
          status=$(echo "$response" | grep -o '"status":"[^"]*' | cut -d'"' -f4)
          cpu_usage=$(echo "$response" | grep -o '"usage_percent":[0-9.]*' | head -1 | cut -d':' -f2)
          memory_usage=$(echo "$response" | grep -o '"usage_percent":[0-9.]*' | tail -1 | cut -d':' -f2)
          
          echo "✅ Full health check passed"
          echo "📈 Health Score: $health_score"
          echo "🔄 Status: $status"
          echo "💻 CPU Usage: ${cpu_usage}%"
          echo "🧠 Memory Usage: ${memory_usage}%"
          
          # ارسال گزارش سلامت به تلگرام
          if [ "$health_score" -lt 70 ]; then
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="⚠️ SERVICE DEGRADED! 
💻 Vortex AI Service
📊 Health Score: $health_score%
🔶 Status: $status
💻 CPU: ${cpu_usage}% | 🧠 Memory: ${memory_usage}%
⏰ Time: $(date +'%H:%M')
🔧 Check required!"
          elif [ "$health_score" -lt 85 ]; then
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🔸 Service Warning 
💻 Vortex AI Service  
📊 Health: $health_score%
💻 CPU: ${cpu_usage}% | 🧠 Memory: ${memory_usage}%
⏰ $(date +'%H:%M')"
          fi

      - name: 💾 Smart Memory Management
        run: |
          echo "🧠 Checking memory usage..."
          
          resources=$(curl -s "$SERVICE_URL/api/health/resources")
          memory_usage=$(echo "$resources" | grep -o '"memory_percent":[0-9.]*' | cut -d':' -f2)
          cache_stats=$(curl -s "$SERVICE_URL/api/health/cache/stats")
          cache_size=$(echo "$cache_stats" | grep -o '"total_size_mb":[0-9.]*' | cut -d':' -f2)
          
          echo "💾 Memory Usage: ${memory_usage}%"
          echo "📦 Cache Size: ${cache_size}MB"
          
          # اگر حافظه بالا بود، اقدام کن
          if (( $(echo "$memory_usage > 75" | bc -l) )); then
            echo "⚠️ High memory usage detected - performing cleanup..."
            
            # پاک‌سازی کش
            curl -X POST "$SERVICE_URL/api/health/cache/clear"
            
            # پاک‌سازی کش نرمال‌سازی
            curl -X POST "$SERVICE_URL/api/health/normalization/clear-cache"
            
            # گزارش اقدام
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🧹 MEMORY CLEANUP PERFORMED! 
💻 Vortex AI Service
📊 Memory Usage: ${memory_usage}%
📦 Cache Cleared: ${cache_size}MB
🕒 Time: $(date +'%H:%M')
✅ System optimized"
          fi

      - name: 🌐 IP & Security Checkup
        run: |
          echo "🛡️ Performing security checkup..."
          
          # دریافت اطلاعات امنیتی
          security_report=$(curl -s "$SERVICE_URL/api/health/debug/security/overview") || security_report="{}"
          recent_ips=$(curl -s "$SERVICE_URL/api/health/debug/security/recent-ips") || recent_ips="{}"
          
          # بررسی IPهای مشکوک
          suspicious_ips=$(echo "$recent_ips" | grep -o '"suspicious":[0-9]*' | cut -d':' -f2) || suspicious_ips=0
          
          if [ "$suspicious_ips" -gt 0 ]; then
            echo "🚨 Suspicious activity detected: $suspicious_ips IPs"
            
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🚨 SECURITY ALERT! 
💻 Vortex AI Service
🔍 Suspicious IPs: $suspicious_ips
🌐 Recent activity detected
🕒 Time: $(date +'%H:%M')
🔒 Review security logs"
          else
            echo "✅ No security issues detected"
          fi

      - name: 🎉 Send Success Notification
        if: success()
        run: |
          echo "✅ All health checks passed"
          
          # فقط اولین اجرای صبح گزارش موفقیت بفرست
          if [ $(date +%H) -eq 09 ] && [ $(date +%M) -lt 10 ]; then
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="✅ VORTEX AI - ALL SYSTEMS GO! 
💻 Service Status: Healthy & Responsive
📊 Monitoring: Active
🕒 Last Check: $(date +'%H:%M')
🎉 Automated systems operational!"
          fi

      - name: 🛡️ Advanced Auto Recovery
        if: failure() && steps.health_check.outcome == 'failure'
        run: |
          echo "🚨 Starting auto-recovery sequence..."
          max_retries=3
          recovery_successful=false
          
          for attempt in $(seq 1 $max_retries); do
            echo "🔄 Recovery attempt $attempt of $max_retries..."
            
            # ۱. پاک‌سازی کش
            echo "🧹 Clearing cache..."
            curl -s -X POST "$SERVICE_URL/api/health/cache/clear" || true
            sleep 2
            
            # ۲. ریست سیستم نرمال‌سازی
            echo "🔄 Resetting normalization..."
            curl -s -X POST "$SERVICE_URL/api/health/normalization/reset-metrics" || true
            sleep 3
            
            # ۳. تست سلامت
            echo "🏥 Testing health..."
            response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" "$SERVICE_URL/api/health/ping")
            http_status=$(echo "$response" | grep "HTTP_STATUS" | cut -d':' -f2)
            
            if [ "$http_status" -eq 200 ]; then
              echo "✅ Auto-recovery successful on attempt $attempt!"
              recovery_successful=true
              
              # ارسال نوتیفیکیشن موفقیت
              curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
                -d chat_id="$TELEGRAM_CHAT_ID" \
                -d text="🔄 AUTO-RECOVERY SUCCESSFUL! 
💻 Vortex AI Service
✅ Recovered on attempt $attempt
🛠️ Actions: Cache cleared, Systems reset
🕒 Time: $(date +'%H:%M:%S')
🎉 Service is back online!"
              break
            else
              echo "❌ Recovery attempt $attempt failed (HTTP $http_status)"
              sleep 5  # صبر کن قبل از تلاش بعدی
            fi
          done
          
          if [ "$recovery_successful" = false ]; then
            echo "💥 All recovery attempts failed!"
            
            # ارسال گزارش خطای مفصل
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🔴 CRITICAL: AUTO-RECOVERY FAILED! 
💻 Vortex AI Service
❌ All $max_retries recovery attempts failed
🚨 Service remains unavailable
⏰ Failure Time: $(date +'%Y-%m-%d %H:%M:%S')
📞 Immediate manual intervention required!"
            
            exit 1
          fi

      - name: 📊 Generate Weekly PDF Report
        if: success() && github.event_name == 'schedule'
        run: |
          # فقط روزهای دوشنبه ساعت ۱۰ صبح
          if [ $(date +%u) -eq 1 ] && [ $(date +%H) -eq 10 ]; then
            echo "📈 Generating weekly PDF report..."
            
            # دریافت داده‌های گزارش
            weekly_data=$(curl -s "$SERVICE_URL/api/health/debug/reports/performance?days=7") || weekly_data="{}"
            
            # ایجاد گزارش متنی
            report_text="📊 VORTEX AI - WEEKLY REPORT
Generated: $(date +'%Y-%m-%d %H:%M')
Service: $SERVICE_URL

📈 PERFORMANCE METRICS:
- Average Health Score: $(echo "$weekly_data" | grep -o '"avg_health_score":[0-9.]*' | cut -d':' -f2 || echo "N/A")%
- Total Requests: $(echo "$weekly_data" | grep -o '"total_requests":[0-9]*' | cut -d':' -f2 || echo "N/A")
- Success Rate: $(echo "$weekly_data" | grep -o '"success_rate":[0-9.]*' | cut -d':' -f2 || echo "N/A")%
- Avg Response Time: $(echo "$weekly_data" | grep -o '"avg_response_time":[0-9.]*' | cut -d':' -f2 || echo "N/A")ms

💾 RESOURCE USAGE:
- Peak CPU: $(echo "$weekly_data" | grep -o '"peak_cpu":[0-9.]*' | cut -d':' -f2 || echo "N/A")%
- Avg Memory: $(echo "$weekly_data" | grep -o '"avg_memory":[0-9.]*' | cut -d':' -f2 || echo "N/A")%
- Cache Hit Rate: $(echo "$weekly_data" | grep -o '"cache_hit_rate":[0-9.]*' | cut -d':' -f2 || echo "N/A")%

🚨 ALERTS SUMMARY:
- Critical Alerts: $(echo "$weekly_data" | grep -o '"critical_alerts":[0-9]*' | cut -d':' -f2 || echo "0")
- Warnings: $(echo "$weekly_data" | grep -o '"warnings":[0-9]*' | cut -d':' -f2 || echo "0")

🎯 RECOMMENDATIONS:
1. Optimize cache TTL settings
2. Monitor memory usage trends
3. Review error patterns
4. Check security logs regularly"

            # ارسال به تلگرام
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="$report_text"
            
            echo "✅ Weekly report sent to Telegram"
          fi

      - name: 📝 Final Status Log
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 Workflow completed successfully at $(date)"
          else
            echo "💥 Workflow failed at $(date)"
          fi
