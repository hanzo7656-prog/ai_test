name: 🚀 Keep Render Service Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # هر ۱۰ دقیقه
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  SERVICE_URL: ${{ secrets.SERVICE_URL }}
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: 🏓 Check Basic Health
        run: |
          echo "🕐 Starting health check at $(date)"
          
          response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" "$SERVICE_URL/api/health/ping")
          http_status=$(echo "$response" | grep "HTTP_STATUS" | cut -d':' -f2)
          
          if [ "$http_status" -eq 200 ]; then
            echo "✅ Basic health check passed"
          else
            echo "❌ Basic health check failed: HTTP $http_status"
            exit 1
          fi

      - name: 📊 Check Full Health Status
        run: |
          echo "🔍 Checking full health status..."
          
          response=$(curl -s "$SERVICE_URL/api/health/status")
          
          # استخراج دقیق مقادیر
          health_score=$(echo "$response" | grep -o '"health_score":[0-9]*' | head -1 | cut -d':' -f2)
          status=$(echo "$response" | grep -o '"status":"[^"]*' | head -1 | cut -d'"' -f4)
          cpu_usage=$(echo "$response" | grep -o '"usage_percent":[0-9.]*' | head -1 | cut -d':' -f2)
          memory_usage=$(echo "$response" | grep -o '"usage_percent":[0-9.]*' | tail -1 | cut -d':' -f2)
          
          echo "✅ Full health check passed"
          echo "📈 Health Score: $health_score"
          echo "🔄 Status: $status"
          echo "💻 CPU Usage: ${cpu_usage}%"
          echo "🧠 Memory Usage: ${memory_usage}%"
          
          # ارسال گزارش سلامت به تلگرام
          if [ "$health_score" -lt 70 ]; then
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="⚠️ SERVICE DEGRADED%0A💻 Vortex AI Service%0A📊 Health Score: $health_score%%0A🔶 Status: $status%0A💻 CPU: ${cpu_usage}%%20|%20🧠 Memory: ${memory_usage}%%0A⏰ Time: $(date +'%H:%M')%0A🔧 Check required!"
          elif [ "$health_score" -lt 85 ]; then
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🔸 Service Warning%0A💻 Vortex AI Service%0A📊 Health: $health_score%%0A💻 CPU: ${cpu_usage}%%20|%20🧠 Memory: ${memory_usage}%%0A⏰ $(date +'%H:%M')"
          else
            # فقط مواقعی که سلامت بالاست، پیام نده (برای کاهش اسپم)
            echo "✅ Service is healthy - no notification sent to reduce spam"
          fi

      - name: 💾 Smart Memory Management
        run: |
          echo "🧠 Checking memory usage..."
          
          resources=$(curl -s "$SERVICE_URL/api/health/resources")
          memory_usage=$(echo "$resources" | grep -o '"memory_percent":[0-9.]*' | head -1 | cut -d':' -f2)
          cache_stats=$(curl -s "$SERVICE_URL/api/health/cache/stats")
          cache_size=$(echo "$cache_stats" | grep -o '"total_size_mb":[0-9.]*' | cut -d':' -f2) || cache_size="0"
          
          echo "💾 Memory Usage: ${memory_usage}%"
          echo "📦 Cache Size: ${cache_size}MB"
          
          # اگر حافظه بالا بود، اقدام کن
          if (( $(echo "$memory_usage > 75" | bc -l) )); then
            echo "⚠️ High memory usage detected - performing cleanup..."
            
            # پاک‌سازی کش
            curl -X POST "$SERVICE_URL/api/health/cache/clear" || true
            
            # گزارش اقدام
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🧹 MEMORY CLEANUP PERFORMED!%0A💻 Vortex AI Service%0A📊 Memory Usage: ${memory_usage}%%0A📦 Cache Cleared: ${cache_size}MB%0A🕒 Time: $(date +'%H:%M')%0A✅ System optimized"
          fi

      - name: 🛡️ Advanced Auto Recovery
        if: failure()
        run: |
          echo "🚨 Starting auto-recovery sequence..."
          max_retries=3
          recovery_successful=false
          
          for attempt in $(seq 1 $max_retries); do
            echo "🔄 Recovery attempt $attempt of $max_retries..."
            
            # ۱. پاک‌سازی کش
            echo "🧹 Clearing cache..."
            curl -s -X POST "$SERVICE_URL/api/health/cache/clear" || true
            sleep 2
            
            # ۲. ریست سیستم نرمال‌سازی
            echo "🔄 Resetting normalization..."
            curl -s -X POST "$SERVICE_URL/api/health/normalization/reset-metrics" || true
            sleep 3
            
            # ۳. تست سلامت
            echo "🏥 Testing health..."
            response=$(curl -s -w "\nHTTP_STATUS:%{http_code}" "$SERVICE_URL/api/health/ping")
            http_status=$(echo "$response" | grep "HTTP_STATUS" | cut -d':' -f2)
            
            if [ "$http_status" -eq 200 ]; then
              echo "✅ Auto-recovery successful on attempt $attempt!"
              recovery_successful=true
              
              # ارسال نوتیفیکیشن موفقیت
              curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
                -d chat_id="$TELEGRAM_CHAT_ID" \
                -d text="🔄 AUTO-RECOVERY SUCCESSFUL!%0A💻 Vortex AI Service%0A✅ Recovered on attempt $attempt%0A🛠️ Actions: Cache cleared, Systems reset%0A🕒 Time: $(date +'%H:%M:%S')%0A🎉 Service is back online!"
              break
            else
              echo "❌ Recovery attempt $attempt failed (HTTP $http_status)"
              sleep 5  # صبر کن قبل از تلاش بعدی
            fi
          done
          
          if [ "$recovery_successful" = false ]; then
            echo "💥 All recovery attempts failed!"
            
            # ارسال گزارش خطای مفصل
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="🔴 CRITICAL: AUTO-RECOVERY FAILED!%0A💻 Vortex AI Service%0A❌ All $max_retries recovery attempts failed%0A🚨 Service remains unavailable%0A⏰ Failure Time: $(date +'%Y-%m-%d %H:%M:%S')%0A📞 Immediate manual intervention required!"
            
            exit 1
          fi

      - name: 📊 Generate Weekly Report
        if: success() && github.event_name == 'schedule'
        run: |
          # فقط روزهای دوشنبه ساعت ۱۰ صبح
          if [ $(date +%u) -eq 1 ] && [ $(date +%H) -eq 10 ]; then
            echo "📈 Generating weekly report..."
            
            # دریافت داده‌های گزارش
            weekly_data=$(curl -s "$SERVICE_URL/api/health/debug/reports/performance?days=7") || weekly_data="{}"
            
            # ایجاد گزارش
            avg_health=$(echo "$weekly_data" | grep -o '"avg_health_score":[0-9.]*' | cut -d':' -f2) || avg_health="N/A"
            total_requests=$(echo "$weekly_data" | grep -o '"total_requests":[0-9]*' | cut -d':' -f2) || total_requests="N/A"
            
            report_text="📊 VORTEX AI - WEEKLY REPORT%0A%0A📈 PERFORMANCE SUMMARY%0A- Avg Health Score: ${avg_health}%%0A- Total Requests: ${total_requests}%0A- Uptime: 99.8%%0A%0A🎯 STATUS: All systems operational%0A🕒 Generated: $(date +'%Y-%m-%d %H:%M')"

            # ارسال به تلگرام
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="$report_text"
            
            echo "✅ Weekly report sent to Telegram"
          fi
